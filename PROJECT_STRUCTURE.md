# هيكل المشروع (Project Structure)

## 📁 البنية الحالية المحسّنة

```
src/
├── main.ts                    # نقطة دخول التطبيق
├── app.module.ts              # الوحدة الرئيسية
├── app.controller.ts          # Controller الرئيسي
├── app.service.ts             # Service الرئيسي
│
├── config/                    # ملفات الإعدادات
│   ├── app.config.ts          # إعدادات التطبيق (Port, Environment, etc.)
│   ├── database.config.ts     # إعدادات قاعدة البيانات
│   └── jwt.config.ts          # إعدادات JWT
│
├── common/                    # الملفات المشتركة
│   ├── constants/             # الثوابت المشتركة
│   │   └── index.ts
│   ├── decorators/            # Decorators مشتركة
│   │   └── index.ts
│   ├── filters/               # Exception Filters
│   │   └── http-exception.filter.ts
│   ├── interceptors/          # Interceptors
│   │   └── transform.interceptor.ts
│   ├── pipes/                 # Custom Pipes
│   │   └── validation.pipe.ts
│   └── utils/                 # Utility Functions
│       └── index.ts
│
├── types/                     # الأنواع والواجهات المشتركة
│   └── index.ts
│
├── auth/                      # وحدة المصادقة
│   ├── auth.module.ts
│   ├── auth.controller.ts
│   ├── auth.service.ts
│   ├── decorators/
│   │   ├── current-user.decorator.ts
│   │   └── roles.decorator.ts
│   ├── dto/
│   │   ├── login.dto.ts
│   │   ├── register.dto.ts
│   │   ├── refresh-token.dto.ts
│   │   └── verify-2fa.dto.ts
│   ├── guards/
│   │   ├── jwt-auth.guard.ts
│   │   └── admin.guard.ts
│   └── strategies/
│       └── jwt.strategy.ts
│
├── users/                     # وحدة المستخدمين
│   ├── users.module.ts
│   ├── users.service.ts
│   └── schemas/
│       └── user.schema.ts
│
└── migrations/                # Migrations
    ├── 20240101000000-create-users-table.ts
    ├── 20240102000000-add-refresh-token.ts
    └── run-migrations.ts
```

## ✨ التحسينات المطبقة

### 1. **تنظيم ملفات الإعدادات (Config)**
   - فصل إعدادات قاعدة البيانات في `database.config.ts`
   - فصل إعدادات JWT في `jwt.config.ts`
   - فصل إعدادات التطبيق في `app.config.ts`
   - استخدام `registerAs` من NestJS Config

### 2. **مجلد Common للملفات المشتركة**
   - **Constants**: الثوابت المشتركة (ROLES, HTTP_STATUS)
   - **Interceptors**: Transform Interceptor لتنسيق الاستجابات
   - **Filters**: Exception Filter لمعالجة الأخطاء
   - **Pipes**: Custom Validation Pipes
   - **Utils**: دوال مساعدة مشتركة

### 3. **مجلد Types**
   - تعريف الأنواع والواجهات المشتركة
   - Pagination Types
   - API Response Types

### 4. **تحسين main.ts**
   - إضافة Global Prefix
   - إضافة Global Interceptors
   - إضافة Global Filters
   - استخدام ConfigService بدلاً من process.env مباشرة

### 5. **تنظيف البنية**
   - حذف المجلدات الفارغة
   - تنظيم أفضل للملفات

## 🎯 المزايا

1. **قابلية الصيانة**: بنية واضحة ومنظمة
2. **قابلية التوسع**: سهولة إضافة ميزات جديدة
3. **إعادة الاستخدام**: ملفات مشتركة قابلة لإعادة الاستخدام
4. **أفضل الممارسات**: اتباع معايير NestJS
5. **Type Safety**: استخدام TypeScript بشكل أفضل

## 📝 ملاحظات

- جميع الإعدادات الآن في مجلد `config/`
- الملفات المشتركة في مجلد `common/`
- الأنواع المشتركة في مجلد `types/`
- كل وحدة (module) منفصلة ومستقلة

